## 组件事件 - Event

- **Event装饰器，此装饰器能修饰此函数为字段组件的事件**


```typescript
defineField()
  .component({
    id: 'email',
    component: 'input',
  })
  .events({
    onChange(){
      // do something
    }
  })

```
