## 组件属性 - Props

- **Prop装饰器，此装饰器不仅能修饰此属性为字段的组件props，也能使其具有响应化（signal）**

```typescript
defineField()
  .component({
    id: 'email',
    component: 'input',
  })
  .props({
    placeholder: '请输入邮箱',
    value: 'email',
  })
  .build()
```
